#!/usr/bin/env python
from distutils.core import setup

setup(name='dattolib',
      version='${PACKAGE_VERSION}',
      package_dir={'': '${CMAKE_CURRENT_BINARY_DIR}'},
      packages=['dattolib'])

